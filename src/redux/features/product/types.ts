export type TProduct = {
  _id: string;
  brand: string;
  category: string;
  createdAt: string;
  description: string;
  images: string[];
  model: string;
  name: string;
  price: number;
  rating: number;
  stock: number;
  updatedAt: string;
};
